# 星枢 Sync Hub — 数据汇入管道与渐进式披露改进方案

> 附录 E（v1.4）· 2026-08-06 固化
> 状态：评审通过，待开工（H1→H4）
> 性质：冻结契约。通过条件原文冻结不得修改，顺序不可变（H1 未完成不得开 H2）

---

## 一、背景

渐进式披露如何处理「切割过的语段」，判定条件怎么界定。

场景：大量数据汇入 Hub → 语义切割 → 类型归档 → 分级处理 → 知识图谱 + LLM wiki。
数据敏感度界定是核心。

## 二、原设计骨架（评审保留部分）

### 2.1 切割的语义单元：chunk 必须是「语义完整的语段」

切割不能是字节切片。切点判定按优先级：

1. **结构边界**：标题/段落/列表/代码块（markdown 或换行簇）
2. **话题转折**：embedding 相似度骤降（相邻窗口 cos < 0.75 即切）
3. **长度上限**：兜底 512 token，仅在无自然边界时使用

每个 chunk 是独立披露单元，且永远知道自己属于哪个父文档。

### 2.2 chunk 建模：披露级别继承父文档，只降不升

新增 `document_chunks` 表（或 memory_pool 加列）：

```
chunk_id | parent_doc_id | piece_index | content | summary
source_agent_id | trust_level | tainted_at              ← S3a 字段复用
disclosure_level | sensitivity_score | chunk_hash        ← 新增
```

**核心规则：chunk 的披露级别 = min(父文档级别, 内容自身判定)**。
父文档是 summary 的，任何 chunk 不能是 full——切段不能绕过父级约束（否则等于把文档拆开偷渡）。

### 2.3 敏感度判定链（6 维度，fail-closed，命中即停）

| 维度 | 判定条件 | 命中 → 级别 |
|------|---------|------------|
| 1 来源信任 | `trust_level == UNTRUSTED`（S3a）| 强制 NONE + taint |
| 2 PII 正则 | 身份证/手机/银行卡/护照/密钥串 | 强制 NONE（证据不落库）|
| 3 机密词库 | 合同价/客户名单/密码/内部代号 | SUMMARY 上限 |
| 4 写入者角色 | owner 是 worker 且含业务数据 | SUMMARY 上限 |
| 5 父文档级别 | parent 的 disclosure_level | 继承 |
| 6 类型 | kind: fact(默认) / secret(最高) / todo(最低) | 按类型映射 |

判定顺序 1→6，命中即停（第一条命中就是最终级别），sensitivity_score 落库供审计。

### 2.4 防拼接泄露（同源约束，原设计——评审指出有洞，见 4.3）

- 检索返回 chunk 时带 `parent_hint: {doc_id, total_chunks}`
- 单次请求披露同源 chunk 数量 > 50% 时按父文档级别重新判定（降级）
- 图谱节点 = 父文档（不是 chunk），chunk 只在全文检索层可见

### 2.5 分级处理 → 图谱/LLM wiki

```
汇入 → 切割 → 敏感度判定 → 三路分流：
  NONE       → 只进审计，不进图谱/wiki
  SUMMARY    → 图谱节点可见(title+summary)、wiki 页面生成
  FULL       → 图谱+wiki 全文
```

N4 已做图谱节点按 requester 过滤，wiki 生成同理过滤——复用现有逻辑。

---

## 三、设计评审：5 个必须堵的洞（2026-08-06）

### 洞 1：规则 2「PII 强制 NONE + 证据不落库」自相矛盾

chunk 的 content 本身要不要落库？

- 若「证据不落库」指连 content 都不存 → 该条记忆等于丢弃，写入方无感知 → 「我以为存了」的数据丢失
- 若 content 落库而仅审计不落证据 → NONE 级内容仍躺在库里，防篡改链和备份都会带出它

**修复（E.1）**：content 落库但打 NONE + PII 命中标记（命中类型 + 位置偏移 + 掩码后样本入审计，原始串不落审计），同时给写入方返回「已接收但已锁定」的明确回执（`locked: true`）。

**PII 判定时机**：判定发生在切割后，但正则可能跨 chunk 边界（如身份证被切到两个 chunk）。
→ PII 预扫必须在**切割前的父文档全文**上跑一遍，命中区间映射回 chunk。

### 洞 2：判定链与 disclosure.py 现有 8 规则的关系没定义（最大架构风险）

会出现两套判定：旧 8 规则（请求时）+ 新敏感度链（写入时）。两处逻辑独立演化必然漂移。

**正确定位（E.2）**：
- 敏感度链 = **写入时的级别上限打标**（决定 chunk 存成什么级别）
- 8 规则 = **读取时的请求方判定**（决定这个请求方能拿到什么级别）
- 读取判定必须再叠一次 `min(请求方判定, chunk 存储级别)`

**承载**：敏感度链本身用 A3 的「规则表化」承载——它是**第 9 条规则**，不是平行宇宙。
A3 的模拟器天然能模拟敏感度判定。A3 未完成时先用独立规则表实现，不阻塞。

### 洞 3：防拼接的「>50% 重新判定」作用在单次请求上，挡不住慢速拼接

低权限方每次只查 30%，分 10 次查完，单请求判定永远不触发。

**修复（E.3）**：按 `(requester, parent_doc_id)` 做**跨请求累计窗口**（24h 滑窗），
累计披露比例超阈值即对该 requester 就此文档降级 + 审计事件 + 可选告警。

另外 `parent_hint.total_chunks` 本身泄露文档结构（对手知道还差几块）→
hint 只给「存在关联内容」布尔量，**不给总数**。

### 洞 4：切分规则缺三条工程细节

1. **最小块合并**：<50 token 的碎片并入相邻块——否则列表项/短段落爆出大量噪声 chunk，每个都过一次分类器，成本爆炸
2. **阈值标定**：相邻窗口 cos < 0.75 的阈值要用真实语料标定后**冻结进配置**，不能写死——不同 embedding 模型分布差异很大
3. **chunk_hash 幂等重入**：同一文档重复汇入时按 hash 跳过已判定 chunk——否则重汇入 = 重复分类器开销 + 审计洪水

### 洞 5：三路分流缺「级别变更回放」

机密词库更新、trust_level 升级（S3b 后可能变）、父文档级别被管理员调整——已入库 chunk 的级别会过期。

**修复（E.7）**：重判定作业——规则/词库变更 → 受影响 chunk 标记 stale → 低峰批量重跑分类器 → 级别变化入审计 + 图谱/wiki 同步摘除或补发。没有这条，词库维护就是摆设。

---

## 四、修订版方案（v1.4 增改）

### 保留（原设计正确骨架）

- 三级切点判定（结构 → embedding 转折 → 512 token 兜底）
- 继承只降不升、fail-closed 取最高敏感度、命中即停
- 图谱节点 = 父文档、三路分流复用 N4 过滤

### 修复的 5 个洞

| # | 修复 | 要点 |
|---|------|------|
| E.1 | PII 判定移到**切割前全文预扫** | 跨边界身份证不会被切成两半漏检；content 落库但锁 NONE；审计只记掩码样本（命中类型+位置偏移+掩码后样本）；写入方收到 `locked: true` 回执——不静默丢弃 |
| E.2 | 敏感度链并入 A3 规则表体系 | 定位为「写入时打标」，与读取时 8 规则取 min——杜绝两套判定漂移；A3 未完成时先用独立规则表实现，不阻塞 |
| E.3 | 防拼接改**跨请求滑窗** | 24h，按 requester×父文档累计 >50% 降级 + 审计 + 通知；`total_chunks` 从响应中移除（本身泄露结构）|
| E.4 | 工程细节三条 | 最小块合并（<50 token）；cos 阈值真实语料标定后冻结进配置；chunk_hash 幂等 |
| E.5 | 级别变更重判定 | stale 标记 + 低峰批量重跑 + 向量/图谱/wiki 同步 |

### E.6 额外抓出的隐蔽洞：NONE 级 chunk 不能建 ChromaDB 向量

**NONE 级 chunk 不建 ChromaDB 向量**——否则语义检索会绕过披露判定直接把内容送回来。
SUMMARY 级只对 **summary 字段**建向量（不建 content 向量）。

---

## 五、开工顺序（H1→H4，不可变）

| 阶段 | 内容 | 出口标准 |
|------|------|---------|
| H1 | chunker + document_chunks 表 | 三级切点 + 最小块合并 + chunk_hash 幂等，单测全绿 |
| H2 | 敏感度链（写入时打标，第 9 规则）| **硬断言：对审计库 grep 原始 PII 串必须零命中**；写入方 locked 回执 |
| H3 | 防拼接（跨请求 24h 滑窗）| 慢速拼接模拟（10 次 × 30% 累计）触发降级 + 审计 |
| H4 | 分流 + 重判定（E.7）| 词库变更 → stale → 重跑 → 图谱/wiki 同步摘除/补发，端到端验证 |

H2 出口标准硬断言：**对审计库 grep 原始 PII 串必须零命中**。

---

## 六、涉及文件

- `disclosure.py` — 读取时判定（8 规则链 + min 叠 chunk 存储级别）
- `hub_core.py` — 汇入管道入口、knowledge_graph（N4 复用）
- `db.py` — document_chunks 表 DDL + 增量迁移
- `models.py` — chunk/汇入请求模型、敏感度规则表配置
- `routes.py` — 汇入端点、检索端点（parent_hint 改造、滑窗累计）
- 新模块：chunker / sensitivity（PII+机密词分类器）/ 重判定作业

---

## 附录 F：三件待办修正（2026-08-06 用户审定）

### K1 换真 embedding（同意先做）

三个坑（冻结）：
1. **选型双档**：bge-small-zh-v1.5（~100MB）随离线包默认分发；bge-m3（~2.2GB）高配可选。落在方案 4.2 的 embedding.provider 抽象上（当前 EMBEDDING_MODEL 配置项存在但从未被消费——需先建 provider 抽象）
2. **旧向量全部作废**：维度 384→新模型维度，全量重建走 E.7 同款 stale 批处理；重建期间检索降级 ILIKE（现有降级链复用，memory_pool/search 三级降级已有）
3. **E.2 切点阈值 0.75 不可迁移**：那是词袋分布下标定的，换模型必须重新标定，否则切割质量可能退化


### K1 收尾（v1.7 用户审定）

1. **验收测试集**（tests/test_embedding_synonyms.py）：50 组中文同义词 + 10 组跨语言，
   参数化 + `@pytest.mark.requires_sentence_model`，hasher 模式自动 skip。
   模型就位后 `pytest -m requires_sentence_model` 一条命令出验收结论（测试先于证据）。
2. **rebuild 上线时机**：维度切换期间检索走 ILIKE 降级（功能可用但质量下降），
   必须选业务低峰执行；执行前先 `hub-cli backup`（迭代 1 产物），失败可回滚。
### K2 实体抽取

铁律（冻结）：
- **先分级再喂 LLM**：NONE 级 chunk 不送 LLM（尤其云端模型碰敏感内容是安全事故）
- 抽取结果走 **wiki_inbox 同款审查队列**——LLM 幻觉实体不能污染图谱
- 实体/关系继承证据 chunk 的级别，图谱下钻仍过披露判定

### K3 机密词库

- 不是功能开发，是**交付物补齐**（机制 E.7 reclassify 已有）
- 缺《初始化模板》：通用 30 词 + 分行业起始包 + 维护责任人约定
- 词库导出/查看本身也要走审批门
