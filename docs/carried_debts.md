# carried_debts 台账

格式：`| 编号 | 来源阶段 | 描述 | 严重度 | 去向 |`

| 编号 | 来源阶段 | 描述 | 严重度 | 去向 |
|------|---------|------|--------|------|
| CD-001 | 预登记（D1） | Hindsight（HCS 服务未启用 + 网络超时） | 高 | 关闭（决策 D1，已砍掉） |
| CD-002 | P0-S1 | 无（S1 通过，工具调用/回显真实数据） | — | — |
| CD-003 | P0-S2 | 无（记忆写入/检索通过） | — | — |
| CD-004 | P0-S3 | 无（任务状态机 created→scheduled→completed + 事件日志通过） | — | — |
| CD-005 | P0-S4 | 无（通知创建 + stderr 推送帧 + stdout 无污染通过） | — | — |
| CD-006 | P0-S5 | 无（30/30 断网重连 + 恢复后通知可达通过） | — | — |
| CD-007 | P0-观察 | e2e1_demo.py 使用 deepseek-v4-pro（思考模型），独立运行不受影响，仅记录 | 低 | 观察（不影响 P0 结果） |
| CD-008 | P1-T1-2 | wiki approve 端点只改 inbox 状态不发布页面（页面文件缺失时「发布可查」不成立） | 中 | 已修复（approve 后触发 sync(force=True) 重建页面），routes.py 本轮 |
| CD-009 | P3-T3-1 | 配对链路三处断点：①/pair/exchange 路由未注册（孤儿）②accept 本地查配对码表必失败（码在对方表）③exchange 无发起方 agent 记录（remote_agent_id 空） | 高 | 已修复：注册 exchange 路由 + 移除本地查表 + pairing_codes 加 agent_id_a 列（含迁移）+ exchange 响应返回 agent_id_a |
| CD-010 | P3-T3-3 | UDP 发现死代码：服务从未启动（hub_core 只留 None）+ discover 端点调不存在的 get_peers + 单机广播不环回 | 高 | 已修复：多播通道（239.255.43.21 同机环回）+ hub_core 实例化启动 + discover 用实例 peers + SERVER_PORT 配置 + hub_id 稳定化（hashlib+端口区分同机多实例）。实测生产 Hub discover 发现 3061 测试 Hub |
| CD-011 | P5 | 跨 Hub 披露泄露：accept 把 remote_agent_id 误存为自身 → 披露规则 1（requester==owner）判 FULL，worker 跨 Hub 可见机密全文 | 高 | 已修复：accept 不传 remote_agent_id（Hub 从 exchange 响应 agent_id_a 取真实对方）。实测 worker 查询机密 0 结果、白名单后可见、假 key 403（7/7） |
| CD-013 | 安全底线 P0 | MCP `/mcp` SSE 无认证（方案锚点表记「已有 Bearer」实测不存在）；P0 allowlist 放行 /mcp | 中 | 登记待做：FastMCP 挂载点前置 Bearer 校验（复用 hub_token） |
| CD-014 | 安全底线 P0 | `/ws/buffer`、`/ws/shared/{doc_id}`、`/ws/shared/watch/{doc_id}` 三通道无任何认证（仅 /ws/dashboard 与 /ws/{agent_id} 有 query api_key） | 高 | 已修复（P1 7da5015）：五通道统一首帧鉴权（D3），4401 拒未认证连接，认证前零注册 |
| CD-015 | 安全底线 P0 | test_l6_auth.py 为 pass 注释桩（无真实断言），已被 test_auth_matrix.py 替代 | 低 | 保留观察（矩阵覆盖） |
| CD-012 | P5 | 跨 Hub 调用端缺失：proxy/disclose 只有被调端，无调用端封装；exchange 不存对方 Hub 地址（remote_hub_url 空）→ 单向链路 | 中 | 已修复：/team/disclose/remote 调用端端点（查表+直调）+ exchange 透传 own_hub_url 落库 |
| CD-016 | 稳定性轮 P2-T2-3 | semantic_search 端点（disclosure.py）硬依赖 ChromaDB 无降级 | 中 | 已修复（加固轮 P0 12cafa1）：三路径降级 SQLite 关键词 + degraded 标记，T0-1~T0-3 全绿 |
| CD-017 | 稳定性轮 P2-T2-1 | knowledge_upsert 入队路径 _record_trace 同步 SQLite 写阻塞事件循环——200 并发峰值 31s | 中 | 已修复（加固轮 P1 3ce5b25）：攒批队列 + to_thread + 索引，200 并发 peak 3.59s <5s，T1-1~T1-6 全绿 |
| CD-018 | 加固轮 P2 | 真实渠道联调（钉钉/SMTP 真实发送各 1 次）——负责人未提供凭据 | 低 | 待办（D3 可选轨，提供凭据后补做，不算失败） |
| CD-019 | 功能完整性轮 P1 | 看板阻塞徽标 UI 视觉确认——数据链路已验（GET /tasks blocked_by + app.js 渲染） | 低 | 已关闭（2026-08-02 视觉复核通过：mimo 识别看板卡片显示「🔴 等待: KB-DEMO-A」徽标，端到端渲染闭环） |
| CD-020 | 防御审计 2026-09-03 | register/bootstrap 用请求 role 覆盖已存在 agent——Agent 端 bootstrap 写死 worker，每次连接把 manager/orchestrator 静默降级（知识写入 403 且无「角色被重置」提示） | 中 | 已修复（Hub 侧 register 保留服务端 role 不降级不提权 + Agent 端 payload 去 role，3 测试，双仓 commit） |
| CD-021 | 防御审计 2026-09-03 | gateway_read_log 只增不删（每次网关读 +1 行，无保留策略）——检索 COUNT/ORDER BY 随行数变慢，审计页被审计数据拖死；events 表有清理而此表没有 | 中 | 已修复（2026-09-03：_run_cleanup 增读审计清理，RETENTION_READLOG_DAYS=90 独立 try，3 测试；见桌面文档 9-03 防御审计轮） |
| CD-022 | 防御审计 2026-09-03 | audit/*.jsonl（memory_pool/transport）append-only 无轮转——磁盘占用 + verify 全量重读变慢 | 低 | 已修复（2026-09-03：JsonlRollingChain 超 20MB 轮转（微秒归档名防同秒覆盖）+ verify_windows 多段校验（锚 end 回落=段切换 + 无锚段推进），篡改/删除归档均可检出；4 测试，见桌面文档 9-03 防御审计轮） |
| CD-023 | 防御审计 2026-09-03 | hub.agents 内存 dict 与 DB 双写，直写 DB 不生效——两真相源必有一个过期，register/role 类 bug 反复源于此 | 中 | 已修复（XS-002 a55465c，2026-09-08：心跳全量重载身份字段，dict 过期 ≤ 心跳周期；判定上下文显式化 requester_info/owner_info override，跨 Hub 虚拟身份不再注入 dict（XS-012）） |
| CD-024 | 防御审计 2026-09-03 | 事件循环 B 类：sqlite 同步 .execute 473 处/30+ 文件（非热点长尾）——数据量增长后任意无索引查询变慢即冻结事件循环（延迟炸弹） | 低 | 登记（docs/event-loop-blockers.md 已文档化；热点已 to_thread；建议给随业务增长的表补索引 + 慢查询护栏） |
| CD-025 | 开放准入 B1 2026-09-06 | S1K data_domain 叠加层无载体列——disclosure 域降级判 memory.department/document_chunks.department，但两表均无 department 列（唯一非空来源是员工/虚拟身份的 agent 记录）→ 数据域限制实际不生效（含 1e 员工模板 __dept__/__project__） | 中 | 已修复（XS-001 af4d86d，2026-09-08）：读时派生——记忆域 = 显式 department 键 → owner 部门（agents/employee_accounts）→ 空=公共区；判定 fail-closed（域外一律降 METADATA），tests/test_xs001_data_domain.py 7 用例 |
| CD-026 | 开放准入 B1 复查 2026-09-06 | arch-site 边车 scoped key 换装——盘查实证前端零活跃数据调用(hubRequest/gatewayRead/ingest 均无调用点), 唯一流量是免鉴权 /readyz 轮询, 无 key 可换 | 低 | 待办(arch-site 数据面 B3+ 落地时随页面定义签发受限 key + 服务端注入, 不提前造凭据) |
| CD-027 | S7（四维审查） | require_approval_for_full 死配置删除（2026-09-09 T1-6）：披露 FULL 无审批门，原配置项误导管理员，删除+example 同步；如需审批门另立需求 | 中 | 已修复（T1-6：hub_core 默认值删除 + config.example.yaml/4 处测试默认 dict 同步 + 加载路径 pop 剥离残留键） |
| CD-028 | S7（四维审查） | db.py 裸 ALTER 冻结进 alembic 0002（2026-09-09 T2-3）；此后改表走 alembic revision | 低 | 已修复（0002_freeze_incremental_alters 覆盖现库与 0001 全量差异：13 表+1 索引+20 列；db.py ALTER 段保留幂等兜底并加冻结注释；shared_workspace 运行时 ALTER 移除；空库/现库副本硬等式差异 0） |
| CD-029 | P4（四维审查） | 异常底线 T2-6(2026-09-09)：核心 64 处静默 except + E722 清零补日志，ruff(E722) CI 门禁；BLE001 盲捕存量 258 处(带日志)+F/E 基础集 1163 处登记，随重构逐步放宽 | 低 | 已修复(Hermes 收尾, kimi 配额中断) |
| CD-030 | D-1（3-7）2026-09-10 | shared_workspace._init_db 内联 DDL 与实际 schema 不一致：SHARED_DOCS_DDL 只建基础列，而 create_doc 直插 trust_level/source_agent_id/tainted_at（由 alembic 0002 添加）→ 未跑 `alembic upgrade head` 的全新库上 create_doc 抛 OperationalError: table shared_docs has no column named trust_level（T2-3 冻结 ALTER 时遗留） | 中 | 待修：内联 DDL 补 3 列（与 0002 对齐），或明确「新库必须走 alembic」并在 _init_db 检测缺列时显式报错 |
| CD-031 | D-2（3-8）2026-09-10 | wiki 审批缺角色门：POST /api/v1/wiki/inbox/{id}/approve（routes_wiki.py:245）只需"已认证"，无角色门，而它把内容 trust_level 由 external 升 internal（源码注释标 S3）；同仓 n1_delete 审批却有门（routes_n1.py:172-173 仅 manager/orchestrator）——两通道审批人要求不一致，任意 worker Agent 可自升信任级 | 中 | **已修复（2026-09-14）**：`routes_wiki` approve/reject 补审批人角色门 `_wiki_reviewer_gate()`——manager/orchestrator 或 hub_token（控制台）放行，worker → 403（与原只有认证、零门的状态相反）；与同仓 n1 通道要求对齐，`tests/test_approval_matrix.py` 的「固化现状」断言同步改为worker 403 / manager 放行 / hub_token 放行 + 跨通道一致性（原 test_cross_channel_worker_asymmetry 已删，新 test_cross_channel_worker_both_403）；NO_AUTH 开发态仍全放行；approve/reject 同门。14 passed |
| CD-032 | D-2（3-8）2026-09-10 | 旧消息格式兼容窗口过期未下线：known_limitations L0-COMPAT-001 的 compat_deadline=2026-08-28 已过期，但 envelope.is_legacy_flat 兼容分支仍在 routes.py:646 生效（两套消息并行） | 低 | 待定：旧 Agent 端已冻结、待新 harness 替换，兼容分支是否关闭待新客户端就绪后评估；本项已写进 known_limitations.py 的 note 字段 |
| CD-033 | D-3（3-4）2026-09-10 | 网关旁路待收编 18 项（其中 15 项**未做披露剥离**的内容读取直返正文）——清点表见 `docs/gateway-bypass-inventory.md`；高危头部：hub-agent/chat 与 chat/history（handler 无身份/角色门，LLM 工具直查全库正文）、knowledge/from-memories、knowledge 与 knowledge/{id}（知识库全表正文）、memory 与 memory/search、wiki/page 与 wiki/search(/hybrid)、shared/docs/{doc_id}；另有灰区：federation/snapshot/{kind} 整表 SELECT * 无剥离、wiki/export 任意持证可拉全 wiki 未剥离全文、网关 kind=memory 为 keep/drop 而非按级别降级截取 | 中 | 待收编（本任务只清点登记，未动手——收编会动 routes_*.py，与 D-7/D-8 routes 拆分冲突，须排其后） |
| CD-034 | D-4（3-6）2026-09-10 | 审计链防重写验收有前提：外部锚（XS-004 webhook）**默认未配**（`AUDIT_ANCHOR_URLS` 空），且全仓**无任何自动把远端锚取回比对本地链头**的机制——`verify_anchor()` 只比同机同目录的 `audit/anchor.txt`（有 DB 写权限者顺手可改），`verify_all()` 不含 anchor；丢尾侧：审计连接 `synchronous=NORMAL`（audit_chain.py:104），断电/内核崩溃可能丢「上次 checkpoint 之后」的一段尾（进程崩溃不丢，且链前缀自洽致本地自检查不出，仅外部锚可检出） | 中 | 评估记录见 `docs/audit-chain-durability-assessment.md`；建议 R1（生产必配外部锚 + 文档声明本地快照不作依据）+ R2（审计连接改 FULL，低频写可接受，不动 db.py 全局热路径）待拍板；R3 远端锚回拉比对可选 |
| CD-035 | D-5（3-5a）2026-09-10 | 依赖分层（chromadb / sentence-transformers / langchain 三件套移入新建 requirements-vector.txt）+ ChromaDB/langchain 缺失降级启动（L2）：hub_core 模块级 import 改 try/except（缺失时 chromadb=None 并复用既有 _chroma_collection=None 降级路径）、routes_hubagent 4 处 langchain 调用点返 degraded 而非 500；CI/Docker 仍装两套（行为不变）。**numpy/scikit-learn 经 grep 实证为核心模块级依赖（db.py:18-19 等），保留核心**。残留：`routes_n1.py:100` 同类 ImportError 未降级（白名单外，随 routes 拆分处理） | 低 | 已修复（D-5，Commit 见 git log）；**残留项现状（2026-09-14）**：D-7/D-8 routes 拆分已完成，但 `routes_n1.py:100` 的同类 langchain ImportError 不在 D-8 归属表内、仍未处理 → 顺延至 3-4 网关收编或 routes 收口轮 |
| CD-036 | D-6（3-5b）2026-09-14 | 资产出仓 `examples/` 后暴露的打包/构建上下文缺口：① 仓库**无 `.dockerignore`**，`Dockerfile` 的 `COPY . .` 会把 `examples/arch-site/node_modules`（实测 243M 未跟踪）连带 dist/build 一起塞进构建上下文（构建变慢、镜像体积与泄密面变大）；② `sync_hub.spec` 被 `.gitignore` 的 `*.spec` 忽略、不入库，而它内含关键打包决策（datas 只带 dashboard + examples/showcase；不携带 config/ 与 chroma_db/ 的凭据与隐私理由），仅存于本机 | 低 | **已修复（2026-09-14）**：① 新增仓库根 `.dockerignore`（排除 node_modules/dist/build/chroma_db/backups/audit/wiki/data-trunk/*.db/*.spec/docs/*.docx/.hermes/缓存等，`COPY . .` 不再把 examples/arch-site/node_modules(实测 243M) 带进构建上下文）；② `sync_hub.spec` 入库（`.gitignore` 加 `!sync_hub.spec` 例外），打包决策（datas 只带 dashboard + examples/showcase，不带 config/chroma_db 的凭据与隐私理由）随仓可溯 |
| CD-037 | D-8（3-2b）2026-09-14 | 端点迁出后的**只登记不清理**项（按 D-8 任务书 §6）：`routes.py` 内已不被本文件使用但保留的 import（`routes_shared._ws/_shared_watchers/_broadcast_shared_update`、`transport_audit.*`、`envelope.*`、`HTMLResponse` 等）与陈旧段注释（`# ============ M3: Memory Pool 增强 ============` 等）；`routes_ws.py` 内搬入的静默 except 日志字符串仍带旧行号（如 `routes silent-except @446`） | 低 | 观察/待清理：留待 3-4 网关收编或专门的 routes 收口轮一并处理；现在清理会与在途改动争同一批文件。**D-9 同类登记**：`hub_mixins/shadow/common.py` 保留了原 import 块中在 common 内未直接使用的 `collections/sqlite3/threading/time`；`hub_mixins/shadow/` 内原 6 处 LF-only 行按字节原样保留、未统一（行尾统一属独立清理项） |
| CD-038 | D-8 后实测（为 D-10/D-11 预登记）2026-09-14 | **db 门面尚未覆盖的存量调用点基线**：AST 实测「async 函数内直接同步访问 SQLite」= **612 处，分布在 118 个 async 函数 / 29 个生产文件**（top：hub_mixins/tasks.py 62、routes_automation.py 58、hub_mixins/ingest.py 51、hub_core.py 46、disclosure.py 33；其中 563 处所在 async 函数内无 to_thread）。这是 CD-024「事件循环延迟炸弹」的量化现状 | 低 | 既定处置：**D-10（6685e87）门面底座已落地**（`db_facade.py` 唯一入口 + 慢查询护栏，`/health` 顶层已观测 stats），口径脚本已产出 `tools/db_call_sites.py`（基线 53 文件 / 65 connect·537 execute·6 executemany / 608 处）； **已修复（D-11 db1d59e）**：D-10 门面底座（6685e87）已落地（`/health` 顶层观测 stats，口径脚本 `tools/db_call_sites.py` 基线 53 文件 / 608 处）；D-11 已把热点 7 文件（memory / ingest / knowledge / notifications / tasks / disclosure / disclosure_ops）42 个 async 函数内的直接 SQLite 调用全部改走 `db_facade`（多语句/事务经 `run_in_conn(write=True)` 保原子性），新增 AST 反向断言门禁 `tests/test_facade_migration.py`；**长尾约 76 个 async 函数（hub_core / routes_* / shared_workspace 等）按既定决议随 PG 迁移一起换** |
| CD-039 | D-10/D-11 性能验收 2026-09-14 | **200 并发「不劣于 3.59s 基线」未取得同口径证据**：本次在**独立测试 Hub（3061 + 全新空库）**上跑 P2_CONC=200（P2_SKIP_CHROMA=1），缓冲路径得到 peak_enq_lat=**5.599s**、locked/timeout/fallback 全 0；但 ① 该 harness 的 db_new/lost 计数硬编码读仓库生产库（DB_PATH=sync_hub.db），对临时库无效 → lost=1665 为假阳性；② 3.59s 出自 CD-017（3ce5b25）在生产库 + 原 P3 harness 的口径，两者不可直接比；③ D-11 迁移的 7 个文件**不含**缓冲/知识入库路径（hub_mixins/buffer.py 未纳入热点），该路径代码未变 | 低 | **已关闭（2026-09-14 同口径复跑）**：结论 = **D-10/D-11 无劣化**——同 DB 快照 + 同 harness + 同 hygiene worktree 下，HEAD 中位 **4.507s** vs D-10/D-11 前身 e73f9da **4.574s**（差 −0.07s，落噪声内；D-11 本就未触碰 hub_mixins/buffer.py）；但相对 CD-017 基线同口径复现值 **3.383s**（3ce5b25 自身复现，口径有效），HEAD 慢 **+1.12s（+33%）** → 漂移来自 2026-08-02→09-14 的 156 提交累积，与 D-10/D-11 无关，另立 CD-040。验收文档 `docs/d10-d11-perf-recheck-2026-09-14.md`；证据 `E:\星枢-待办\_sync\cd039\`（18 轮全 0 locked/timeout/丢失） |
| CD-040 | D-10/D-11 同口径复跑 2026-09-14 | **200 并发缓冲路径相对 CD-017 基线漂移 +1.12s**（3.383s → 4.507s，+33%）：同 DB 快照/同 harness/同 hygiene worktree 下基线提交 3ce5b25 自身复现 3.383s（口径有效），HEAD 4.507s、D-10/D-11 前身 e73f9da 4.574s → 漂移与 D-10/D-11 无关，出在 2026-08-02→09-14 的 156 提交之间；候选成因已定位到 `routes.py:255-292 _agent_quota_ok()`——鉴权中间件对**每个已认证请求**调它，函数体内 `sqlite3.connect`+SELECT agent_quotas+close 全同步跑在事件循环里（未走 to_thread/db_facade），200 并发下与写缓冲`BEGIN IMMEDIATE` 争锁（CD-024 形态；属 CD-038 登记的 routes_* 长尾）。样例：`sqlite3.connect(CONFIG.DB_PATH)` 每请求一次、表无行时纯属空转 | 中 | **已修复（0a5d091，2026-09-14）**：O4 配额改内存快照（`routes_common.agent_quotas_snapshot`，TTL 30s + 写点失效）+ 认证列集进程内缓存（默认开）+ token→row 缓存（**默认关**，`SYNC_HUB_AUTH_ROW_CACHE=1` 开，TTL 8s，rotate_keys 挂钩失效）。同口径同快照配对实测（worktree/2 轮）：修复前 4.745·4.769 → 默认档 3.957·4.144 → 行缓存开档 **2.761·3.066**；吞吐 1817-1846 → 2332 → 3483-3498。验收文档 `docs/d10-d11-perf-recheck-2026-09-14.md` §七。**残余（待用户拍板）**：默认档 ~4.0s 仍高于冻结口径 3.59s，达标依赖显式开启行缓存；行缓存把「凭据变更立即生效」放宽为「≤TTL」，与 tests/test_s1_auth_provider.py 的轮换·过期语义冲突（3 用例），故未改默认——选项：① 维持默认严格（口径按「开启性能开关后达标」记）② 默认开 + 调测试语义 ③ 只开 TTL 更短（如 3s）+ 把撤销/轮换全部纳入失效钩子后默认开。**修复期间实测还发现**：key_scopes 每次认证也各查一次库（未纳入本次修复） |
| CD-041 | 门禁 2026-09-14（推内部仓时抓出） | `test_auth_matrix` 跨用例端口竞态 + 压测产物放大：该文件逐个 spawn/kill 同一 3062 端口 Hub，而全路由扫描用例会给每个路由发真实请求、其中 `GET /api/v1/wiki/sync` 在 wiki 6429 页时要 23-24s → 上一 Hub 未退干净时新 Hub 绑定失败，fixture 的健康探针打到**旧 Hub** 误判就绪、旧 Hub 退出后该用例 `/health` 拿 0（`test_allowlist_health_200` 全量必红、单跑绿、热态序列绿）。**已修复**：fixture 起前等端口彻底释放 + 就绪判定校验「监听 PID == 本次 spawn 进程」+ 收尾等释放（8 passed / 15.4s，原 1 failed / 43s） | 低 | 已修复（本轮）。**顺带沉淀口径**：压测/压测类任务收尾必须清产物——删 `wiki/**/p2k-*` 生成页 + 清 `knowledge_base` 里 p2k 行，否则 wiki sync 成本只涨不降、门禁长期贴 5s 悬崖（本轮实测 sync 24s → 0.4s）；已写入 project-xingshu skill |
| CD-042 | CD-040 修复期发现 2026-09-14 | `GET /api/v1/wiki/sync` 全量同步**同步阻塞式**：wiki 6429 页时单次 23-24s （清到 415 页后 0.38s），且每次都要重写全部页面文件 + 6030 条向量；任何客户端调用必然 5s 超时（`test_auth_matrix` 全路由扫描就因此超时并引发跨用例端口竞态，见 CD-041） | 中 | **已修复（2026-09-14）**：`GET /api/v1/wiki/sync` 不再让调用方卡死——默认路径改 `asyncio.to_thread`（返回体不变，向后兼容；不再占事件循环）；新增 `background=1` 后台任务（单飞：进行中重复触发返 `already_running`）+ `GET /api/v1/wiki/sync/status` 轮询；控制台 `dashboard/wiki.html` 按钮改「触发即返回 + 1.5s 轮询状态」，toast 显示完成/失败/进行中。新增 `tests/test_wiki_sync_route.py`(5)。注：同步本身仍是全量重建（页数多时耗时长），增量/限流作为后续可选优化；日常靠「压测产物收尾清理」保持页数低位 |
