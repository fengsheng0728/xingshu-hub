# 星枢 Sync Hub — 核心依赖
# 安装: pip install -r requirements.txt
#
# 钉版说明：以下版本以 2026-09-09 本机（Python 3.14）pip freeze 实测可运行版本为基准，
# 用 ~= 锁定兼容范围（允许补丁级升级，禁止跨次版本漂移）。
# Dockerfile 中的旧 pin（fastapi==0.104.1 等）需另行对齐（另一任务），本文件未同步。

# Web 框架
fastapi~=0.136.3
uvicorn[standard]~=0.49.0
aiohttp~=3.13.5
websockets~=15.0.1

# 数据模型
pydantic~=2.13.4
pyyaml~=6.0.3

# 数据库 & 向量搜索
numpy~=2.4.6
scikit-learn~=1.9.0
# 向量栈（chromadb / sentence-transformers / langchain）见 requirements-vector.txt（可选，~2GB）

# HTTP 客户端
httpx~=0.28.1
requests~=2.33.0
websocket-client~=1.9.0

# MCP / CRDT 协作
mcp~=1.26.0
pycrdt~=0.14.2
pycrdt-store~=0.1.5
pycrdt-websocket~=0.16.4
