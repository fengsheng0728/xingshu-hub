# 星枢 Hub Dashboard UI 重做方案（v1.2 轻拟物版）

> 定位：服务端直出 HTML → 轻量工程化前端；视觉与信息架构整体重做；风格=**轻拟物 Soft Skeuomorphism**（拟物质感语言：凸起/凹陷光影、物理隐喻、材质层次；但**数据密集区保持扁平**——拟物只用于"少而关键"的元素，详见 §2.0 适用范围铁律）。
> 约束继承：铁律 5（静态资源本地化，桌面/内网必须完全离线，禁 CDN 外链）；铁律 6（任何 markdown 渲染过 DOMPurify）。

---

## 一、信息架构重做（IA）

### 1.1 现状问题

6 页（首页/展示/聊天/知识/报表/Wiki）是**功能视角**组织——"我们有什么功能就有什么页"。管理后台应按**角色任务**组织："管理员来这是为了看什么、管什么、批什么"。展示/聊天页与管理职责无关，应降级或移除出主导航。

### 1.2 新导航（4 区 10 页）

```
┌─ 概览 ─────────────────────────────
│  ① 总览 Overview      健康+告警+关键指标+待办（原首页重做）
│  ② 活动 Activity      实时事件流（WS 推送：任务/披露/审批/联邦）
├─ 治理 ─────────────────────────────
│  ③ 访问与权限 Access  账号+角色模板+API key+例外租约（附录 K/J 落点）
│  ④ 披露治理 Disclosure 规则链编辑器+模拟器+shadow 差异报告（A3 落点）
│  ⑤ 审计中心 Audit     审计检索+hash chain 校验+审批记录+导出
├─ 数据 ─────────────────────────────
│  ⑥ 知识 Knowledge     知识库+图谱（原知识页升级，N4/K2 落点）
│  ⑦ Wiki              页面管理+收件箱审查（review_queue 入口之一）
│  ⑧ 记忆 Memory        记忆池检索+版本+可视化（原记忆面板迁入）
├─ 系统 ─────────────────────────────
│  ⑨ 运行 Ops           配额用量/缓冲积压/降级状态/备份状态/Agent 在线（O1-O5 落点）
│  ⑩ 集成 Integrations  外部系统连接器管理（ERP/CRM/财务…见 §七，预留位）
│  ⑪ 设置 Settings      server config+通知通道+词库管理+联邦节点
```

**移除/降级**：展示页（showcase）移出主导航保留直链；聊天页降级为"Hub Agent 调试"入口收进设置区——管理后台里放聊天是定位混乱。

### 1.3 全局骨架

```
┌──────────────────────────────────────────────┐
│ Topbar: 面包屑 │ 全局搜索(⌘K) │ 告警铃铛 │ 账号 │
├────────┬─────────────────────────────────────┤
│ 侧栏    │  内容区                                │
│ 4区10页 │  ┌─ KPI 行（每页顶部，4-6 个 stat）     │
│ 折叠    │  ├─ 主面板（表格/图/编辑器）            │
│ 分组    │  └─ 抽屉（右侧滑出详情，不跳页）         │
└────────┴─────────────────────────────────────┘
```

三个全局模式（所有页一致）：
- **列表 → 抽屉详情**：任何表格行点击右侧滑出详情抽屉，不离开列表上下文；
- **待办聚合**：Topbar 铃铛聚合三类待办（审批/审查队列/告警），角标计数 = review_queue 待办 + 审批 pending + 未恢复告警；
- **⌘K 命令面板**：跳页 / 搜 Agent / 搜记忆 / 执行动作（"吊销 key""触发备份"——带确认）。

---

## 二、设计规范（Design Tokens）

### 2.0 拟物适用范围铁律（先定边界，再谈风格）

重度拟物与高密度数据界面冲突（行高 32px→56px，一屏信息量腰斩）。因此按区域分级：

| 区域 | 拟物程度 | 说明 |
|---|---|---|
| **KPI 卡片 / 健康卡 / 状态指示** | ★★★ 全拟物 | 仪表盘物理隐喻的天然场景：凸起卡片、内嵌表盘、实体指示灯 |
| **主操作按钮 / 开关 / 滑块 / 分页器** | ★★ 轻拟物 | 凸起 + 按压反馈（active 态凹陷），有"可按"的物理暗示 |
| **侧栏选中项 / Tab 激活项** | ★★ 凹陷 | 选中="按下去"，未选=平——方向感明确 |
| **表格 / 列表 / 表单输入区** | ☆ 扁平 | 数据区零拟物，仅保留 1px border 与 hover 态；信息密度不让步 |
| **图标** | ★★ 轻拟物 | 线性图标 + 微渐变/微投影，不用拟真实物图标（齿轮就是齿轮，不是照片级齿轮） |

**判定口诀：一屏超过 10 个的组件不拟物，用户每分钟要点的组件必拟物。**

### 2.1 色彩（亮暗双主题；亮色默认。拟物的光影依赖中性偏暖底色）

```css
/* 亮色（默认）：拟物光影的载体是"有色底"，纯白底上做不出凸起 */
--bg-0: #e8ebf0;   /* 页面底：冷灰，光影画布 */
--bg-1: #eef1f5;   /* 面板：与底色近色相，凸起全靠光影而非色差 */
--bg-2: #f5f7fa;   /* 悬浮/抽屉 */
--border: #d4dae3; /* 仅数据区使用 */
--text-0: #2a3442; --text-1: #5d6b7e; --text-2: #93a0b3;
--accent: #3b6fd4;         /* 沉稳企业蓝 */
--ok: #2fa36b;  --warn: #d99114;  --danger: #d0453e;
/* 披露四级语义色（全站一致，这是产品语言） */
--lv-full: #2fa36b; --lv-summary: #3b6fd4; --lv-metadata: #d99114; --lv-none: #d0453e;

/* 拟物光影对（风格核心，亮主题）：同色相高明暗差 */
--shadow-raised:  6px 6px 12px #c9cfda, -6px -6px 12px #ffffff;   /* 凸起 */
--shadow-raised-sm: 3px 3px 6px #cdd3de, -3px -3px 6px #ffffff;   /* 小元件凸起 */
--shadow-inset:  inset 4px 4px 8px #c9cfda, inset -4px -4px 8px #ffffff; /* 凹陷（选中/按下/表盘） */
/* 暗色（bg-0:#171b21 bg-1:#1e242c）：光影对换为 --shadow-raised: 6px 6px 12px #10131a, -6px -6px 12px #262e38 等，语义色不变 */
```

规则：① **凸起只用双向柔和投影实现，禁深色边框+投影叠罗汉**（那是 2010 年代重拟物，显脏）；② 一屏一个 accent 焦点；③ 披露四级色板是全站统一"产品语言"；④ 亮暗同稿——暗色拟物的光从左上 45° 打，亮暗主题光源方向一致。

### 2.1.1 拟物组件规范（★★★/★★ 区域的实现标准）

- **KPI 卡**：bg-1 + `--shadow-raised` + 圆角 16px；卡内数值区用 `--shadow-inset` 做"嵌入表盘"（数字等宽字体，像刻进面板）；
- **健康指示灯**：10px 圆点，亮=色块+同色外发光（`box-shadow: 0 0 6px currentColor`），灭=`--shadow-inset` 凹坑——物理仪表灯隐喻；
- **按钮**：主按钮 accent 底 + `--shadow-raised-sm`，`:active` 变 `--shadow-inset`（按下去）；次按钮 bg-1 同处理；**禁用态=完全扁平 + 50% 透明度**（"坏掉的按钮按不动"）；
- **开关/滑块**：轨道 `--shadow-inset`，滑块 `--shadow-raised-sm` + 圆形渐变高光；
- **侧栏选中项**：`--shadow-inset` 凹陷 + accent 左缘 3px——"当前页被按进面板里"。

### 2.2 字体与密度

- 中文：Noto Sans SC；西文/数字：Inter；**数字/ID/hash 一律等宽**（JetBrains Mono 或系统 ui-monospace）；
- 密度三档：表格行高紧凑 32px / 默认 40px / 宽松 48px，用户可切（管理员偏好高密度）；
- 字阶：11/12/13/14/16/20/28，七级封顶；页面标题 20，卡片标题 13 大写化处理仅用于西文 label。

### 2.3 组件语言

- **表格是第一公民（扁平区）**：列宽可调、列显隐、排序、粘性表头、行内快捷操作（hover 显现）、批量选择；表格容器本身可以是拟物面板（外框凸起），但**表体行绝对扁平**；
- **状态徽标**：dot + 文字（不纯用色块，照顾色弱）；dot 用指示灯样式（§2.1.1）；
- **空态三件套**：说明 + 主操作按钮 + 文档链接（禁"暂无数据"四个字裸奔）；
- **动效预算**：抽屉/悬停 150-200ms ease-out；按钮按压 80ms（物理反馈要更快）；页面切换无动画；**拟物阴影对性能敏感——阴影元素总数单屏 ≤40 个，超出部分降级为 1px border**；禁用 blur >8px（继承 Agent 端教训）；图表入场无动画（数据即现）。

---

## 三、关键页面设计要点

### ③ 访问与权限（本次最重要的新页）
- 三 tab：账号 / API Key / 例外与租约；
- 账号表格：姓名·邮箱·模板徽标·部门·最近活跃·状态；行点击抽屉=权限解释视图（"为什么能看 X"，脱敏规则轨迹，I.3）；
- 「例外与租约」tab：所有 override 列表 + 到期倒计时 + 一键收回——**例外必须被阳光晒到**的产品化；
- 顶栏 KPI：未映射账号数（fail-closed 命中数）、7 天内到期租约、活跃 key 数。

### ④ 披露治理
- 左：规则链表（启用/禁用/优先级拖拽/shadow 标记）；右：模拟器面板（输入 requester+target → 逐规则命中轨迹渲染，命中规则高亮、级别用四级语义色）；
- shadow 差异报告：双栏对比 enforce vs shadow，差异行高亮。

### ⑤ 审计中心
- 检索条：时间范围 + principal + 动作类型 + 结果码；结果表格 + 行展开原始 JSON；
- 顶部常驻「链完整性」卡片：上次 verify 时间/结果/覆盖条数 + 手动触发 verify 按钮；
- 导出 CSV/JSON（导出动作本身入审计）。

### ① 总览
- 第一行：健康四卡（Hub/DB/向量库/联邦）+ 告警条（未恢复告警滚动条）；
- 第二行：今日披露判定分布（四级堆叠条）、审批 SLA、Agent 在线、缓冲积压；
- 第三行：待办三列（待审批/待审查/待处理告警）——**总览页的本质是"我今天要点哪三件事"**。

---

## 四、技术路线

```
hub_ui/                    # 新增前端工程（Hub repo 内子目录）
  ├─ vite + Vue 3（SFC + Composition API，团队上手成本低于 React，模板语法适合表格密度场景）
  ├─ 状态：Pinia；请求：fetch 封装（Bearer 注入 + 401 跳登录）
  ├─ 图表：ECharts 本地 npm 安装（离线 OK，按需引入）│ 表格：手写（<200 行）或 TanStack Table
  ├─ 无 UI 框架依赖（Element/AntD 都不引——体积大且风格违和，tokens 手写 CSS 变量）
  └─ 构建产物 → hub/dashboard_dist/（FastAPI StaticFiles 挂载，入库 gitignore+发版打包）
```

- **路由**：hash 路由（`/#/access`）——静态托管无服务端 rewrite，深链可直达；
- **构建链**：`npm run build` 产物由 FastAPI 托管，旧 6 页保留一个版本周期作回退（`/legacy/`），新 UI 灰度：server config 开关 `ui.new: true/false`；
- **离线铁律**：全部依赖 npm 本地安装 + 构建产物自包含，运行时零外链；字体 woff2 本地打包；
- **WS 接入**：复用 `/ws/dashboard` 通道，活动流/角标/健康卡实时刷新，断线退化为 30s 轮询（复用现有降级模式）；
- **鉴权**：登录页接 S1-SMB 账号（阶段 1e 产物）；在此之前先用现有 hub_token 登录占位，接口层不变。

## 五、分批交付（每批可独立验收）

| 批 | 内容 | 验收 |
|---|---|---|
| U1 | 工程骨架 + 设计 tokens + 全局骨架（侧栏/Topbar/抽屉/⌘K）+ 总览页 | 离线构建产物 Hub 托管可访问；灰度开关生效；旧页可回退 |
| U2 | 运行 Ops + 活动 Activity（WS 实时流） | 断 WS 自动降级轮询；指标与 /metrics 对齐 |
| U3 | 访问与权限 + 审计中心 | 依赖阶段 1e/1a 接口；权限解释视图、链校验卡片全通 |
| U4 | 披露治理 + 知识/Wiki/记忆三页 | 模拟器轨迹渲染正确；与 A3/A5 接口联调 |
| 全程 | 视觉走查 + 密度/色弱/空态 checklist | 铁律 3：每页真实数据端到端验证，禁 mock 截图充数 |

---

## 七、外部系统集成预留接口（ERP / CRM / 财务管理）

> 定位：**本期只建"门框"，不建"房间"**——连接器框架 + 预留位全部落地，具体 ERP/CRM 适配器有客户合同时再写。目标是未来接入新系统时只写一个适配器文件，不动 Hub 核心。

### 7.1 集成层架构

```
外部系统（ERP/CRM/财务/OA…）
      ↕ 适配器（每系统一个文件，实现 Connector 接口）
┌─────────────────────────────────────────────┐
│ 集成层 Integration Hub（新增模块）              │
│  ① 入站：webhook 接收 / 定时拉取（cron 复用调度引擎）│
│  ② 规范化：统一实体映射 + 字段映射表              │
│  ③ 汇入：走附录 E 管道（切割→敏感度分级→分流）     │
│     —— 集成数据自动获得分级/披露/审计能力，零特例   │
│  ④ 出站：Hub 事件 → 外部系统（复用通知 fan-out）   │
└─────────────────────────────────────────────┘
```

**关键设计决策：集成数据一律走附录 E 汇入管道，taint 标记 `external`（H.2/S3a 体系）**——ERP 里的客户资料进记忆池时自动过敏感度判定（客户名单命中机密词库 → 上限 SUMMARY），集成能力自动继承全部安全体系，这是"预留接口"最重要的正确性保证。

### 7.2 Connector 接口（适配器契约）

```python
class Connector(Protocol):
    name: str                      # "yonyou_u8" / "kingdee_k3" / "salesforce_like_crm"
    def configure(cfg: dict) -> None          # 连接参数（server config 存储，密钥加密）
    def test_connection() -> TestResult       # ⑩页"测试连接"按钮
    def pull(since: datetime) -> Iterator[RawRecord]      # 定时增量拉取
    def handle_webhook(payload: dict) -> list[RawRecord]  # 实时推送（可选）
    def map_entity(raw: RawRecord) -> CanonicalEntity     # 字段映射
    def push_outbound(event: HubEvent) -> None            # 出站（可选）
```

### 7.3 统一实体映射（Canonical Model）

跨系统的同一概念先映射到规范实体，再入管道——避免"ERP 的客户"和"CRM 的客户"在知识库里是两张皮：

| 规范实体 | ERP 对应 | CRM 对应 | 财务对应 |
|---|---|---|---|
| `customer` | 客户档案 | Account/Contact | 往来单位 |
| `employee` | 职员 | User | 报销人 |
| `order` | 销售订单 | Opportunity | 应收单 |
| `invoice/voucher` | — | — | 凭证/发票 |
| `product` | 存货档案 | Product | — |

- 字段映射表存 server config（**可视化编辑，不硬编码**——不同客户同种 ERP 字段也可能定制过）；
- 每条 CanonicalEntity 携带 `source_system + source_id`（溯源）+ `trust_level=external`（taint）。

### 7.4 权限映射（与披露体系对齐）

- 集成数据入池时打**数据域标签**（dept/project，附录 K 双标签）：如 ERP 销售订单 → `dept: sales`；
- 员工查"某客户回款"：走三方取 min——员工账号（本部门 SUMMARY）∧ chunk 存储级（客户资料被机密词库压到 SUMMARY）→ 拿到摘要而非全文，**业务便利与权限边界同时成立**；
- 出站动作（Hub 写回 ERP）一律走审批门 + 审计（写外部系统 = 高危操作，等价 shell 高危级）。

### 7.5 本期交付的"门框"清单

| 交付 | 内容 |
|---|---|
| 后端 | `integrations/` 模块骨架：Connector 注册表、入站 webhook 端点（`/api/v1/integrations/{name}/webhook`，scoped key 鉴权）、拉取调度接入、credentials 加密存储（复用 S2 链外密钥管理） |
| 前端 | ⑩ 集成页：连接器列表（名称/状态/最近同步/记录数）+ "添加连接器"向导占位（ERP/CRM/财务三个预置卡片，状态=未配置）+ 字段映射编辑器 + 测试连接按钮 |
| 文档 | 《适配器开发指南》：实现 Connector 接口 + 丢进目录即注册，含一个 example_connector.py（模拟数据跑通全链路：webhook→映射→E 管道→检索可见） |
| 不做 | 任何真实 ERP/CRM 适配器（用友/金蝶/SAP 等有合同再写）；双向实时同步（先单向入站，出站按需） |

### 7.6 出口标准

- [ ] example_connector 全链路：模拟 webhook → 实体映射 → 敏感度分级（含一条命中机密词的客户记录 → SUMMARY 封顶）→ 检索可见且级别正确；
- [ ] 集成页可完成"配置→测试连接→手动拉取→看到记录数"闭环（用 example connector）；
- [ ] 集成数据 taint=external 溯源链完整（源系统→同步时间→审查状态）；
- [ ] 出站动作被审批门拦截并审计；
- [ ] 新增第二个 dummy connector **不改 Hub 核心任何一行代码**（门框有效性实证）。

---

## 六、给执行 AI 的补充 Prompt 段（追加在 sync-hub-execution-prompt.md 之后用）

```
新增阶段 U（UI 重做），技术约束：
- hub_ui/ 子目录，Vite + Vue3 + Pinia，禁引入 Element/AntD 等 UI 框架，禁 CDN 外链，
  字体与图表库全部本地化（铁律 5）；markdown 渲染过 DOMPurify（铁律 6）。
- hash 路由；构建产物由 FastAPI StaticFiles 托管于 /，旧页保留 /legacy/ 一个版本周期；
  server config 加 ui.new 灰度开关。
- 设计 tokens 按 hub-ui-redesign.md §二严格执行（**轻拟物 Soft Skeuomorphism**：先读 §2.0
  适用范围铁律——一屏超 10 个的组件不拟物、每分钟要点的组件必拟物；表格行绝对扁平；
  凸起只用双向柔和投影，禁深色边框叠投影；亮色默认、暗色同稿且光源方向一致；
  单屏阴影元素 ≤40 个，超出降级 1px border；按钮按压 80ms；禁 blur>8px）。
- 信息架构按 §1.2 四区十一页（含⑩集成页预留位）；展示/聊天页移出主导航。
- 集成层按 §七执行：本期只建门框（Connector 骨架+webhook 端点+集成页+example connector），
  禁写任何真实 ERP/CRM 适配器；集成数据必须走附录 E 管道且 taint=external。
- 每批（U1-U4）交付后跑视觉走查 checklist + 铁律 3 端到端真实数据验证，
  验收表附 commit hash + 截图。
- 依赖管理：U3/U4 若接口未就绪（阶段 1e/1a/A3），先做页面骨架+接口 mock 层，
  mock 层单文件隔离并在接口就绪后删除——mock 不得进入验收证据。
```
