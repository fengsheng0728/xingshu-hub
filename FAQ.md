# Hermes Sync Hub FAQ —— 小公司老板常见疑问

## 1. 为什么不用微信群？群里也能发工单啊

**微信群的问题：**

| 对比项 | 微信群 | Sync Hub |
|--------|--------|----------|
| 信息隔离 | 所有人能看到所有消息 | 客服 A 不知道客服 B 知道什么 |
| 审计 | 无法追溯"谁看了哪条消息" | 谁看了什么、什么级别、什么时间，全部记录 |
| 新员工 | 新人要爬几千条聊天记录 | 按需披露，只给"刚好够用"的信息 |
| 信息过载 | 群消息不断，容易漏掉重要通知 | 任务驱动，Agent 只收到自己相关的 |

**一句话：微信群是广播，Sync Hub 是按需披露。**

---

## 2. 服务器挂了怎么办？我的业务会停吗？

**不会全面瘫痪。** 设计上有两层保障：

1. **Agent 本地可独立运行**：即使 Hub 连不上，每个 Agent 仍然可以正常接待客户、写记忆到本地。Hub 只负责"查别人"和"调度"，不负责"干活"。

2. **记忆写入零依赖**：`write_memory` 不需要 Hub 响应也能继续工作（推荐场景下客户端先缓存本地，Hub 恢复后同步）。

3. **恢复后自动重连**：Hub 重启后，Agent 的心跳机制会自动重连。离线期间的记忆可以补传到 Hub。

**一句话：小二还是能正常接单，只是主管暂时看不到日报。**

---

## 3. 跟共享文件夹 / 共享 Excel 有什么区别？

**共享文件夹的问题：**
- 谁都能看，没有级别控制
- 文件多了找不到，没有搜索
- 没有披露审计，出了事不知道谁泄密

**Sync Hub 的区别：**
- 写入隔离：Agent 写的内容默认只有自己能看到
- 渐进披露：主管只能看摘要，不能看完整对话
- 搜索驱动：写的时候打标签（如"色差"、"橱柜"），查的时候按标签查

**一句话：共享文件夹是"全部公开"，Sync Hub 是"刚好够用"。**

---

## 4. 我们只有 6 个人，有必要搞这么复杂吗？

**有，因为 6 个人的信息量可能比 60 人还大。**

客服行业的现实：
- 一个客服每天接待 20-50 个客户
- 每个客户可能涉及 3-5 次沟通
- 6 个客服一周产生 600-1500 条记录

如果不做隔离和渐进披露：
- 新客服要翻几百条记录才能上手
- 主管被信息淹没，漏掉重要投诉
- 客户信息在自己不知道的情况下被其他人看到

**不需要一开始就用所有功能。** 可以先只用"写入隔离 + 主管看摘要"两个功能，其他等业务规模大了再开。

---

## 5. 部署和维护复杂吗？需要专门请 IT 吗？

**最低要求：一台能跑 Windows/Linux 的电脑，懂基础命令行操作。**

部署方式（按难度从低到高）：

| 方式 | 难度 | 说明 |
|------|------|------|
| 一键启动 | ★☆☆ | `python main.py` 直接跑 |
| Docker 部署 | ★★☆ | `docker-compose up -d` |
| 服务注册 | ★★★ | 写入 systemd/Windows 服务自动启动 |

99 人以内的小公司场景：
- **5-10 个 Agent**：单机 `python main.py` 完全够用
- **20-50 个 Agent**：建议用 Docker 部署（资源隔离）

**不需要专职 IT。** 能把电脑开机、能打开命令行就能跑。

---

## 6. 数据安全怎么保证？会不会泄露给第三方？

**数据全在你们自己的服务器上。** Sync Hub 不依赖任何云服务：

- 数据存在自己服务器的 SQLite 或本地文件
- 不需要注册外部账号
- 没有第三方 API 调用
- 网络通信：只在公司内网，不经过公网

**一句话：你的数据，你的服务器，谁也偷不走。**

---

## 7. 以后业务做大了（超过 99 人）怎么办？

**架构已预留扩展路径：**

| 规模 | 建议方案 |
|------|---------|
| < 99 人 | 单机模式（本方案） |
| 100-500 人 | SQLite → PostgreSQL |
| 500+ 人 | 多 Hub 分片 + 负载均衡 |
| 任意规模 | [可选] ChromaDB 向量搜索 |

渐进式披露的核心设计在所有规模下都有效，底层的数据库和部署方式可以升级。

---

## 8. 我能自己加功能吗？比如自动生成日报？

**可以。** 本案例提供的是"调度框架"，你可以：

1. **加一个日报 Agent**：每天凌晨查询所有 Agent 的记忆摘要，自动汇总成 Excel
2. **加一个通知 Agent**：某个客户投诉升级时，自动给主管发微信提醒
3. **加一个分析 Agent**：按标签统计"色差"类投诉的数量趋势

只需要在 Hub 上注册一个新的 Agent，订阅相应的披露级别即可。

**架构是可扩展的，不是封闭的。**

---

## 9. 有没有实际跑通的案例？

**本案例就是。** 企业的客服场景（6 个客服 + 2 个主管 + 1 个店长）是这个方案的原型场景。

真实业务数据可供参考：
- 客服小王每天写入 15-30 条客户工单
- 主管小李每天查询团队记录 3-5 次
- 店长老陈每周调度处理 5-10 个投诉任务
- 披露升级平均每周 2-3 次（遇到纠纷时需要完整历史）

---

## 10. 能不能先试用一下再决定？

**可以。** 部署和试用流程：

```bash
# 1. 下载
git clone <项目地址>
cd sync-hub-case

# 2. 安装依赖
pip install fastapi uvicorn aiohttp numpy websockets

# 3. 启动 Hub
python main.py

# 4. 另开终端，启动演示
python examples/client.py --demo
```

全程不需要配数据库、不需要注册账号、不需要联网。
如果觉得好用再决定要不要正式用。
